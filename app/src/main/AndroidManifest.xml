<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ch.movie">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Movielist"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".ui.ShowListActivity"
            android:exported="true"
            android:label="@string/title_activity_show_list">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.movieDetailedView.MovieDetailedActivity"
            android:exported="true"
            android:label="@string/title_activity_movie_detailed"
            android:theme="@style/Theme.MovieList.MovieDetail" />
        <activity
            android:name=".ui.tvShowDetailedView.TvShowDetailedActivity"
            android:exported="true"
            android:label="TvShowDetailedActivity"
            android:theme="@style/Theme.MovieList.MovieDetail" />
    </application>

</manifest>